{
  "name": "bca-assist-portal",
  "version": "1.0.0",
  "description": "BCA Assist - LND College Smart AI Portal",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.0.0",
    "framer-motion": "^10.16.0",
    "axios": "^1.6.0",
    "lucide-react": "^0.294.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "dev": "netlify dev",
    "deploy": "eslint src/ && npm run build && netlify deploy --prod"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
